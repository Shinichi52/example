const expt = {
    debug(content) {
        console.debug(content);
    },
    info(content) {
        console.info(content);
    },
    error(content) {
        console.error(content);
    },
    warn(content) {
        console.warn(content);
    }
};

export default expt;